# Verification Guide

**Unverified → Observed → Reproduced → Matched.** Disassembly claims should be tied to exact bytes/addresses or equivalent native identifiers. Matching means the documented build/reassembly criterion is actually met, not merely visually or behaviorally similar.