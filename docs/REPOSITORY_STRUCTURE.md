# Repository Structure

Create areas only when real material exists. Typical verified needs may include `asm/` or `src/`, `data/`, `assets/`, `tools/`, `tests/`, `docs/`, `manifests/`, and non-ROM `patches/`. Native architecture determines substructure. No structure-version roots, migration-history trees, or duplicate canonical homes.