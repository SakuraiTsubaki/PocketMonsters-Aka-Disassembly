# Asset Workflow

Identify exact native source → deterministic extraction → preserve source bytes/indices/palettes/addresses and hashes → create reviewable images where useful → deduplicate only by byte/hash proof → register manifests → validate reconstruction/encoding. Target conversions must remain distinct from the native source layer.