# Research Guide

Record exact build, bank/section/file, address/offset/symbol, bytes or structure observed, inference/hypothesis, tool/command, hashes, and cross-version differences. Incorrect attribution should be corrected explicitly rather than silently propagated.